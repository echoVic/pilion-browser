import{describe,it,expect}from'vitest';import{AgentConfigSchema}from'../src/shared/contracts';describe('agent config',()=>{it('rejects empty executable',()=>{expect(()=>AgentConfigSchema.parse({id:'1',name:'x',command:'',args:[],enabled:true})).toThrow()});it('accepts explicit executable',()=>{expect(AgentConfigSchema.parse({id:'1',name:'x',command:'/bin/x',enabled:true}).args).toEqual([])})});
